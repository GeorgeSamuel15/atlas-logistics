# Documentation only: set in your shell or hosting environment.
# Defaults are appropriate for local development.
PORT=4000
HOST=127.0.0.1
DB_PATH=./data/atlas.sqlite
APP_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174,http://localhost:4000,http://127.0.0.1:4000
# NODE_ENV=production requires HTTPS because session cookies become Secure.
# BOOTSTRAP_EMAIL / BOOTSTRAP_PASSWORD / BOOTSTRAP_NAME are consumed only by npm run bootstrap.
